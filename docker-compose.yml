version: '3'
services:
    jenkins:
        image: jenkins:2.60.3
        build:
            context: .
            dockerfile: Dockerfile
        ports:
          - "29000:29000"
          - "50000:50000"
        environment:
          - CLIENTPORT=29000
        volumes:
          - "./jenkins_home:/var/jenkins_home"